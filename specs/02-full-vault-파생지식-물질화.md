# Spec 02 — Full-Vault 원칙: 내부 파생 지식을 볼트 md로 물질화

> 2026-07-25 · 사용자 지시: "내부적으로 정리하던 문서들 전부 trim해서 규정 둘러보기 한 편에서
> 볼 수 있게(md로 정리하고 md로 보여주고) — Full-Vault-Principle 같은 느낌".
> 관련: CLAUDE.md(단일 진실원천=볼트) · specs/01 P3(정의어 라우팅) · docs/18(Track A/C)

## 1. 원칙

**"파이프라인이 알아낸 지식은 사람도 볼 수 있어야 한다."**
지금까지 도구들이 규정 원문에서 추출한 지식(정의어·기한·준용관계·효력)은 `tools/index/*.json`
파생 인덱스에만 있어 **기계만 읽었다**. 이 중 **사람에게 지식 가치가 있는 것**은 볼트 마크다운으로
물질화해 둘러보기에서 탐색·검색·인용할 수 있게 한다(= 볼트가 유일한 진실원천이라는 아키텍처와 정합).

**⛔ 무엇이든 다 내보내지는 않는다** — 검수 큐·파이프라인 상태 같은 운영 산출물은 노출 시
사용자에게 노이즈이고 RAG 코퍼스를 오염시킨다. 아래 분류가 판단 기준.

## 2. 파생 인덱스 17종 분류 (2026-07-25 실사)

### A. 물질화 대상 — 지식 자산
| 인덱스 | 지식 | 상태 |
|---|---|---|
| `defterms.json`(01j) | 규정 원문 정의어 194종 + 정의 충돌 11 | ✅ **완료** — `30_용어집/규정정의/` 202노트 + **허브 '규정 용어 사전'** |

### B. 이미 전용 UI가 정본 — 노트 중복 생성 안 함
| 인덱스 | 현재 노출 | 판단 |
|---|---|---|
| `approval.json` | `/approval` 결재선 페이지(335규칙 검색·직급 기억) | 매트릭스는 표 UI가 md보다 우월 — 중복 금지 |
| `deadlines.json`·`deadline_labels` | 기한 사전 + 드로어 계산기(.ics) | 계산은 인터랙션 필수 — md 부적합 |
| `byeolji_manifest.json` | 서식 찾기(253종 PDF·HWP) | 이미 완전 노출 |
| `clause_xref`·`graph_analytics` | 문서 드로어 준용칩·개정 파급 | 그래프는 시각화가 정본 |
| `article_status.json` | 드로어 효력 배지 + RAG 삭제 강등 | 조문별 인라인이 맞음 |

### C. 운영 전용 — 볼트 미노출(관리자 화면에만)
`conflict_audit`·`conflict_candidates`(검수 후보) · `byeolji_audit`·`format_scan`·`format_restore`·
`table_integrity`(품질 큐 → /admin) · `law_fetch_state`·`exclude`·`reindex_stale`·`value_store`(파이프라인 상태).
⟹ **사람이 읽을 '지식'이 아니라 '작업 지시'** — 볼트에 넣으면 RAG가 이를 규정처럼 인용할 위험.

## 3. 물질화 규약 (A군 공통)

1. **원문 복사만** — 정의·기한 등 본문은 원문 그대로(⛔절대규칙2 의역 금지), 출처는 `[[문서#조]]` 위키링크
2. **검수상태: 미검수** — 기계 추출물임을 명시 + 노트 하단 "검수 전 초안" 고지(UI 배지와 정합)
3. **사람 확정 보존** — 기존 노트가 `검수상태: 검수완료`면 덮어쓰지 않음
4. **슬러그 충돌 회피** — 볼트 전역 노트명과 겹치면 스킵(CLAUDE.md 규약)
5. **멱등** — 재실행 시 산출 동일하면 파일 무변경(mtime 유지 → 불필요 재색인 방지)
6. **허브 노트** — 개별 노트 + "○○ 사전" 색인 1장(규정별 그룹·충돌 섹션)으로 한 편에서 탐색
7. **골든 관문** — 볼트 증가 = RAG 코퍼스 증가이므로 **회수 A/B로 비퇴행 입증 후** 채택

## 4. 실행 결과 (defterms, 2026-07-25)

도구 `tools/01z_defterm_notes.py`(01j 다음 실행):
- 개별 노트 **194종**(신규 171·기존 자동생성 23 갱신) + 허브 `규정 용어 사전.md`
- 정의형(`form='정의'`)만 물질화 — 약칭 187건은 노이즈라 제외
- 충돌 용어(전자문서 등 11종)는 **정의 전부 병기 + ⚠ 경고 블록**("업무가 속한 규정 기준으로 판단")
- 둘러보기: 용어집 섹션 · 분류 `규정정의` 필터로 진입, 용어 툴팁(`terms-tooltip.json`)도 자동 확장

**검증 관문**: 회수 A/B(kei_regs_v2 vs v3) Hit@1·Hit@5 비퇴행 · 신뢰 게이트 6/6 · 거부 회귀 ·
정의형 라우팅 E2E(중복게재) · 둘러보기 실렌더.

## 5. 향후 확장 시
새 파생 인덱스를 만들 때 §2 기준으로 A/B/C를 먼저 판정하고, A면 §3 규약대로 물질화 도구를
`01z` 패턴으로 추가한다. C를 볼트에 넣지 않는 것이 이 원칙의 절반이다.

## 6. 골든 관문 결과와 결정 (2026-07-25) — **Full-Vault ≠ Full-Corpus**

노트를 볼트에 넣는 것과 **RAG 코퍼스에 넣는 것은 다른 문제**임이 실측으로 확정됐다.

| 구성 | Hit@1 | Hit@5 | 판정 |
|---|---|---|---|
| v2(기존 서빙, 규정정의 31노트 색인) | 74 | 91 | **기준** |
| v3-a: 194노트 **색인 포함** | 68 | 86 | ❌ 정의 노트가 원문 조문을 밀어냄(중복) |
| v3-b: 194노트 **색인제외** | 69 | 84 | ⚠ 아래 분석 |

⚠ 중간 함정 2건(둘 다 실측으로 발각):
- ① 프론트매터 인라인 주석(`색인제외: true  # 사유`)이 값에 붙어 **제외가 무효화**(청크 수 불변으로 발각) → 주석을 코드 쪽으로, 파서는 `#` 앞 토큰만 보게 방어.
- ② 최초 비교가 **P3 라우팅 도입 전 측정치(80/92)와 도입 후를 대조**해 오염 — 동일 코드로 v2를 재측정(74/91)해 공정 비교 복원. *비교 기준은 반드시 같은 코드에서 다시 뜬다.*

**v3-b 손실 7건 분해**: 5건은 골든의 `출처`가 정의 노트 자체라 노트를 빼면 구조적으로 Hit 불가(**문서 정체성 artifact**) — 별도 측정에서 **15/15 문항 모두 정답 내용이 컨텍스트에 보존**됨을 확인(원문 조문이 대신 회수). 나머지 2건은 순위 요동.

**결정(관문 규칙 '신 ≥ 구' 준수)**:
- 서빙 컬렉션은 **kei_regs_v2 유지** — 검증된 상태를 바꾸지 않는다(무회귀 원칙).
- 볼트 노트 194종 + 허브는 **둘러보기 전용으로 유지**(사람에겐 순이득, RAG 무영향).
- `색인제외: true`는 노트에 남긴다 — **다음 재색인부터 유효**. 채택 전 **선행 조건**:
  질문은행의 note-출처 15문항을 **원문 조문 출처로 재지정**(내용 동일 확인됨)해 지표 왜곡 제거.
  그 뒤 재측정해 v2 이상이면 정식 채택.
- ⚠ 재색인 담당자 주의: 이 코드로 재색인하면 정의 노트가 코퍼스에서 빠진다(위 조건 선행할 것).

## 7. 색인제외 **철회** — 증거가 반대했다 (2026-07-25 후속)

§6에서 "질문은행 재지정 후 재평가"를 선행 조건으로 걸고 보류했었다. 그 조건을 이행하고 재측정한 결과 **색인제외를 철회**한다.

**① 선행 조건 이행** — `eval/repoint_note_sourced.py` 신설: 출처가 용어 노트인 15문항을 정의가 실린
원문 조문으로 재지정. ⛔지표 조작 방지 장치 — defterms 바인딩(정의형+약칭형)으로 후보를 찾고
**골든 문장이 그 조문에 실재할 때만**(2-그램 ≥0.9) 옮기며, 실패 건은 손대지 않고 보고. 결과 **15/15 통과**
(14건 1.0 · 1건 0.978), 각 문항에 `출처재지정` 감사 필드 기록.

**② 재평가(재지정된 은행 기준)**

| 컬렉션 | Hit@1 | Hit@5 | 거부 회귀 |
|---|---|---|---|
| v2(현행 서빙, 노트 색인됨) | 68 | **90** | 통과 |
| v3(노트 색인제외) | **70** | 88 | ❌ **실패** |

**③ 기각 사유 — 지표가 아니라 사용자 피해**: 같은 질문을 두 컬렉션에 물어 직접 대조:
- 질문: "임시계정을 발급받기 위해 필요한 개인 정보와 동의서는?"
- **v2** ✅ "이름·메일·핸드폰·회사명·부서명을 지식정보팀에 메일로…"(근거: 스마트연구플랫폼매뉴얼)
- **v3** ❌ "**규정에서 확인되지 않습니다**"(엉뚱한 문서 회수)

Hit@1이 +2 좋아져도 **답을 못 받는 회귀**가 실재하면 채택하지 않는다.

**④ 왜 틀렸나(교훈)**: 용어 노트를 "원문 조문의 중복 복사본"으로만 봤는데, 실제로는 **용어명이 곧 제목**이라
검색 앵커(라벨) 역할을 하고 있었다 — specs/01 P1에서 규정명 라벨이 Hit를 올린 것과 같은 기전이다.
파생 뷰라고 해서 검색 가치가 없는 게 아니다.

**⑤ 조치**: `01z`에서 `색인제외` 선언 제거 → 노트 195개 재생성(잔여 0). **메커니즘(02의 색인제외 지원)은 유지** —
검수 큐·작업 지시 같은 진짜 비지식 문서에는 여전히 유효하다. §2의 A/B/C 분류(무엇을 볼트에 넣나)는 불변이고,
바뀐 것은 "**A군을 색인에서 뺄 것인가**"에 대한 답뿐이다: **빼지 않는다.**

**⑥ 재색인 안전성 확인(함정 제거 완료)** — 선언을 뺐으니 "재색인하면 노트 195개가 코퍼스에 들어가는"
상태가 된다. 그 상태(`kei_regs_v4`, 라벨 on + 노트 포함)를 재지정 은행으로 검증:

| | 현행 v2(노트 31) | **v4(노트 195, = 재색인 결과)** |
|---|---|---|
| Hit@1 | 68 | **69** |
| Hit@5 | 90 | **90** |
| 표적(명패) | 0/2 | **1/2** |
| 신뢰 게이트 | 6/6 | **6/6** |
| 핵심 회귀 케이스(임시계정) | ✅ 응답 | **✅ 응답** |
| MUST_REFUSE | 5/5 | **5/5**(첫 실행 4/5는 재실행에서 5/5 — LLM 비결정성) |

⟹ **재색인해도 현행 이상**임을 확인하고 dev 서빙을 `kei_regs_v4`로 승격(pm2 save). 이제 "서빙 중인 것"과
"재색인하면 나오는 것"이 일치한다 — §7 조치로 만들어진 잠재 함정이 실측으로 닫혔다.

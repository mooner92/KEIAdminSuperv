# 호롱 v2 — Spotify 스타일 리디자인 (2026-07-27 핸드오프)

> **구현 중 브랜치**: `feat/spotify-v2` (3101 dev에서 검증, ⛔ prod 3100 무접촉).
> 원칙·규약 정본은 [`../../docs/design-system.md`](../../docs/design-system.md), v1 시안은 [`../horong/`](../horong/)(수정 금지).

- `HANDOFF.md` — 디자이너 원본 명세(10화면 + PR 6분할). 값의 1차 출처.
- `prototype/호롱 Spotify 리디자인.dc.html` + `support.js` — 인터랙티브 프로토타입(브라우저로 열기).
- `horong-tokens-v2.css` — globals.css 토큰 두 블록의 드롭인 교체본.

## 다루는 규칙 (v1과 동일)
1. 시안을 페이지에 붙여넣지 않는다 — CSS Modules + 시맨틱 토큰으로 재구현.
2. 시안과 코드가 어긋나면 코드가 정본. **단, 이 리디자인은 "디자인 파일을 손보며 진행"(사용자 지시)** —
   구현 중 확정된 변경은 이 폴더의 파일에도 반영해 스냅샷을 최신으로 유지한다(v1과 다른 점).
3. 프로토타입의 jsdelivr 폰트 링크는 프로토타입 한정 — 앱은 self-host 유지(P5).

## 사용자 확정 결정 (2026-07-27)
- **소개 페이지(/about·랜딩 내러티브)는 현행 구성 유지** — "새로 만든 것보다 좋다".
  화면 10(랜딩/로그인) 명세는 **토큰·테마 통일만** 적용하고 구조 재설계는 하지 않는다.
- 워딩은 v2 프로토타입이 옛 표기(규정 찾기·규정집 기준일)를 쓰고 있음 — 구현은
  2026-07-27 확정 워딩(문서 찾기·문서 기준일)을 따른다(프로토타입이 낡은 부분).

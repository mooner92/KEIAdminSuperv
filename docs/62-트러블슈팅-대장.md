# docs/62 — 트러블슈팅 대장

> **목적**: 운영·개발 중 발견한 결함의 **증상→원인→해결→재발방지**를 한곳에 축적한다.
> 사용자 노출 버그는 버그리포트(볼트 _changelog)가 정본이고 여기는 **기술 원인의 정본**.
> 새 항목은 맨 위에 추가(최신순). 세부 코드는 커밋, 여기는 진단 지도만.

| # | 날짜 | 증상 | 상태 |
|--:|---|---|---|
| T8 | 07-24 | 거부형 과잉응답 — 규정 밖 사안에 무관 조문 억지 유추 | ✅ 수정(규칙17) |
| T7 | 07-24 | 자가평가 채점기가 정상 거부·정답을 오답 처리 | ✅ 수정(REFUSAL_RE) |
| T6 | 07-23 | K4 후 수치 게이트가 정상 여비 값 과차단 | ✅ 수정 |
| T5 | 07-23 | 예산운용지침 kordoc 가짜 경계 3개 | 🔍 검수행(게이트 차단) |
| T4 | 07-23 | kordoc 복합문서 표 분해 → 가짜 조문 경계 | 🔍 검수행(게이트 차단) |
| T3 | 07-23 | kordoc 볼드 조문 중첩괄호 누락(제7조) | ✅ 수정 |
| T2 | 07-23 | 조문 경계 카운트가 2배로 측정되는 착시 | ✅ 원인 규명(결함 아님) |
| T1 | 07-23 | 06:00 크론 전량 중단 — 품질게시판 미갱신 | ✅ 수정 |

---

## T9. 거부 판정 정규식의 문맥 무시 — 정상 답변을 거부로 오집계 (T7의 거울상)
- **증상**: Qwythos A/B(07-24)에서 "과잉거부 8건" 집계 — 실물 확인 결과 대부분이 **정답을
  제대로 답한** 답변. "규정에서 **확인된** 총 4,399건"(긍정문)이 `규정에서\s*확인` 패턴에,
  정답 뒤 정직한 부가 안내("서식 내용은 근거에 명시되지 않아 원문 확인")가 `명시되지\s*않`에 매칭.
- **원인**: REFUSAL_RE가 4곳(daily_grade·regress_refusal·ab_model_test·app_api)에 복제된 채
  ①긍정/부정 미구분 ②전체 텍스트 스캔(두괄식 결론이 아닌 꼬리 문구까지 판정). T7이 "정상
  거부→오답 오판"이었다면 T9는 "정상 답변→거부 오판" — 같은 클래스(측정기가 문맥을 안 봄) 재발.
- **해결**: `tools/refusal_detect.py` **단일 정본**(+pms_audit 포함 5곳 교체) — 결론부(첫 문단
  ~200자) 스코프 + 부정형 한정 + 능동형 변형('명시하지 않') 포함. 유닛 12건 전부 실측 실물.
  재채점: 과잉거부 8→**1**(측정 오탐 7), 정답 25→27. 관리자 거부율 통계도 동일 결함 수정.
- **교훈**: **측정기가 틀리면 개선 방향 전체가 오도된다**(임베딩·청킹 탓으로 갈 뻔). 같은
  판정 로직이 여러 곳에 복제되면 결함도 복제된다 — 판정기는 단일 정본 + 실물 유닛으로 고정.
  specs/01 P0.

## T8. 거부형 과잉응답 — 규정 밖 사안에 무관 조문 억지 유추
- **증상**: "구내식당에서 외부인 초대 가능?"·"회의실 음식물 반입?" 등 규정 코퍼스에 없는 사내
  시설·편의 질문에, 챗봇이 거부하지 않고 억지 답변 생성(자가평가 07-24 진짜 오답 2건).
- **원인**: 검색은 항상 top-5를 반환 → 표면 단어만 겹치는 무관 조문(여비규정 제36조 '출장
  초청')이 회수됨 → 모델이 '초대/초청' 겹침을 근거로 간주해 답을 지어냄(절대규칙1 위반 위험).
- **해결**: SYSTEM 규칙17 — 근거 중 어느 것도 질문 사안을 직접 규율하지 않을 때만 거부(시설·
  편의 이용은 코퍼스에 대개 없음). ⚠단 근거가 실제 규율하면 정상 답(과잉 거부 금지). 골든셋
  회귀(regress_refusal.py)로 정상 답변 미파손 확인 — 1차 안이 정상 질문 과잉 거부 → 완화.
- **교훈**: 프롬프트 거부 강화는 **양날** — 반드시 골든셋 회귀로 정상 답변 보존을 검증한다.
  더 안전한 실패는 과잉 거부(절대규칙1: 지어내는 것보다 '확인 안 됨'). docs/58 §6b.

## T7. 자가평가 채점기가 정상 거부·정답을 오답 처리 (게시판 숫자 왜곡)
- **증상**: 07-24 "정답률 89.5%·오답 6". 실제로 오답 6건 중 4건이 채점기 버그 — 정상 거부
  ("명시되어 있지 않습니다")를 오답 처리, 정답을 "A인데 A 아니다"로 오판.
- **원인**: `REFUSAL_RE`가 거부 표현 일부만 인식('확인되지 않'만 있고 '명시/포함되지 않'·'해당
  내용 없음' 등 누락) → 정상 거부를 미인식. 게시판 거부율(app_api)도 같은 정규식이라 동반 왜곡.
- **해결**: REFUSAL_RE 확장(daily_grade + app_api 동기). 재채점 89.5→94.6%, 오답 6→3.
- **교훈**: 측정계(채점기)의 버그는 개선 전체를 무의미하게 만든다 — "오답을 라벨만 보지 말고
  실물을 연다"가 진단 1원칙. 채점 노이즈(LLM 판정 'A=A' 오판)는 값형에 잔존(추가 보정 여지).
  발견 계기 = 사용자 지적("측정만 하면 무의미"). docs/58 §6b.

## T6. K4 재변환 후 수치 게이트 과차단 (대조군A 실패)
- **증상**: K4 적용 직후 신뢰 게이트 대조군A(숙박비 정상 값)에 ⚠ 경고 — 근거(별표 2)에
  100,000이 실존하는데 "근거에서 확인되지 않음" 판정.
- **원인**: 허용집합의 무단위 표 값 수확(`_bare_table_values`)이 **파이프(|) 표 행 전용**.
  kordoc 재변환으로 별표 2가 HTML `<table>`이 되자 무단위 값이 허용집합에서 빠짐.
  회수·절단·표손상은 전부 정상이었음(범인 소거로 확정).
- **해결**: HTML `<tr><td>` 행 수확 추가(태그 제거 후 동일 규칙·순번 열 제외). 게이트 6/6 복구.
- **교훈**: 포맷 전환(파이프→HTML)은 **소비자 전수 점검** 필요 — 렌더러(K1ⓑ)만 챙기고
  수치 게이트를 놓쳤다. 컨텍스트 소비자 목록: 렌더러·수치 게이트·표손상 휴리스틱·value_store.

## T1. 일일 자가평가 크론 크래시 (품질게시판 07-23 미갱신)
- **증상**: 첫 무인 회전(07-23 06:00)이 `daily_gen.py:188 AttributeError`로 중단.
  게시판에 오늘자 미게시. 크론 자체는 정각 실행(로그 확인).
- **원인**: `b.get("출처", {}).get(...)` — `get(k, 기본값)`은 **키 부재 시에만** 기본값.
  거부형 문항은 `출처: null`로 저장되어 `None.get()` 크래시. 전날 첫 회전이 거부형을
  은행에 처음 저장했고, 다음날 무인 회전이 그 은행을 읽다 발병(수동 검증에선 미노출).
- **해결**: `(b.get("출처") or {})` 가드. 동일 패턴 전수 grep — 1곳뿐. (9b7bae7)
- **재발방지**: ⓐ null 가능 필드는 `or {}` 관용구 강제 ⓑ **크론 실패 알림(obs) 부재의
  실측 비용** — 조용히 죽어 사용자가 발견. P3 잔여인 실패 배너를 조기 착수 권장.

## T2. 조문 경계 카운트 2배 착시 (kordoc A/B 측정)
- **증상**: 어댑터 검증에서 `ARTICLE.findall()` 수가 현행 14 vs 어댑터후 41로 과다.
- **원인**: 경계 정규식 `(?=^\s*제\d+조)`의 `\s*`가 **개행을 넘어** 빈 줄 위치에서도
  중복 매칭. kordoc은 마크다운식 빈 줄을 쓰므로 조문당 2~3회 카운트. **실제 분할은
  정상**(02는 split 후 공백 조각을 버림 — 청크 수 16=16 일치).
- **해결/교훈**: 경계 검증은 findall 개수가 아니라 **split 후 라벨 시퀀스**로 잰다
  (`kordoc_adapt.chunk_labels`). 측정 방법의 결함이지 파이프라인 결함 아님.

## T3. kordoc 볼드 조문 정규화가 중첩괄호 제목에서 실패
- **증상**: 기본연구사업 운영규정 43청크 중 **제7조만 누락**(42청크).
- **원인**: 제목이 `제7조(기본연구사업 계획(안) 수립)` — 정규화 패턴 `\([^)]*\)`가
  중첩 괄호의 첫 `)`에서 멈춰 볼드 스팬 매칭 실패.
- **해결**: 괄호 대신 **볼드 닫힘(`**`)을 경계**로 사용(`[^*\n]*`). 라벨 43=43 일치. (bedfc91)
- **재발방지**: parity 게이트(`--parity`)가 라벨 시퀀스 불일치를 exit 2로 차단 —
  이 버그도 게이트가 잡아냈다. 재변환은 반드시 게이트 통과 후 적재.

## T4. kordoc이 복합문서의 일부 표를 문단으로 분해 → 가짜 조문 경계
- **증상**: 운영 표준지침(91쪽, 하위규정 23종 묶음) parity ❌ 180 vs 188 — `제0조`×7
  (인사규정 개정안 예시 표)·`제82조`×1(전자정부법 인용 표 셀) 가짜 청크.
- **원인**: kordoc이 해당 표(권고사항 조치방안 등 복잡 병합 표)를 HTML `<table>`로
  유지하지 못하고 **셀 내용을 문단 조각으로 풀어냄** → 표 안 예시 조문(`제0조(…)`)·인용
  (`제82조 제4호에`)이 줄 시작으로 나와 02 경계에 걸림. 내용 유실은 아님(3-그램 99.5%).
  현행 스택은 같은 내용을 파이프 표 셀에 담아 경계 미발생.
- **해결**: 어댑터로 못 고침(어느 경계가 진짜인지 문맥 필요 — 자동 수정은 내용 조작
  위험). **parity 게이트가 자동 차단**(exit 2) → 사람 검수행. 처리 방침은 docs/61 K3
  (원문 PDF 대조 수동 교정 후 재게이트).
- **교훈**: kordoc 표 품질은 일반 규정 문서에선 우수(병합 셀 rowspan 보존)하나
  **대형 복합문서의 초복잡 표**에선 분해 결함. 문서 단위 게이트가 필수인 이유.

## T5. 예산운용지침(2026, 147K자) 가짜 경계 3개
- **증상**: parity ❌ 181 vs 184 — diff가 뒤쪽(부칙 영역)에 집중.
- **원인**: T4와 동류. 부칙의 타 내규 개정 문구("제41조 중 …을 …으로 한다")가
  kordoc 출력에서 줄 시작 → 가짜 경계. 복합문서(회계규칙+협동연구규정 등 묶음) 특성.
- **처리**: 게이트 차단 → 검수: 가짜 2곳(부칙 개정 문구) 결정적 결합. 잔여 여분 1건은
  **kordoc이 옳은 케이스** — `제3조(적립금의 범위)`가 현행본에선 줄 시작이 아니어서(현행
  파서가 앞 문장에 붙임) 경계 누락, kordoc은 정상 분리. 현행 대비 parity는 "다름=오류"가
  아니라 검수 신호임을 확인한 첫 사례. 소형 조문 문서 5종은 전부 ✅.

---

## 참조
- kordoc 도입 설계·게이트: docs/61 K1~K4, `tools/kordoc_adapt.py`
- 자가평가 운영: docs/58 (P3 크론·§8 골든 동기화)
- 사용자 노출 버그의 정본: 볼트 `90_관리/_changelog/*` (type: bugreport)


## 재색인 중 pkill로 컬렉션을 비워버림 (2026-07-29)

**증상**: dev 검색 전면 불능. `kei_regs` 컬렉션 0개.

**원인**: `02_chunk_and_embed.py`는 **클린 리빌드**다 — 기존 컬렉션을 먼저 비우고 새로 채운다.
그 사이에 다른 작업(HWP 파서 정리)을 하며 `pkill -f`를 썼는데 패턴이 넓어
**재색인 프로세스까지 함께 죽였다.** 비운 뒤 채우기 전에 죽어서 0개가 남았다.

`nohup ... &`로 띄운 것도 툴 호출의 프로세스 그룹이 정리될 때 함께 사라졌다.

**조치**: 백그라운드 도구(`run_in_background`)로 재실행 → 6,028개 정상 적재. 검색 복구 확인.

**교훈**
- 재색인처럼 **'먼저 지우고 다시 채우는'** 작업 중에는 관련 프로세스를 죽이지 않는다.
- `pkill -f <패턴>`은 의도보다 넓게 잡는다. 죽이기 전에 `pgrep -af`로 **무엇이 잡히는지 먼저 본다.**
- 장시간 작업은 `nohup &`이 아니라 **백그라운드 도구**로 띄운다(세션 정리에 살아남고 완료 알림도 온다).
- ⛔ 재색인 전 Chroma 백업은 이래서 필수다(`/tmp/chroma.bak-preerp`가 안전망이었다).

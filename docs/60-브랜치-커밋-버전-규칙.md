# docs/60 — 브랜치 · 커밋 · 버전 규칙 (2026-07-22 확정)

> 사용자 확정: "main 밑에 dev, dev 밑에 기능별 브랜치 + 커밋 규칙 + v1.x.x 버전 관리."

## 1. 브랜치 구조

```
main ──────────●────────●──▶   프로덕션(3100/9000, /KEIAdminSuperv). 승격 시에만 병합+태그
  └ dev ───●──●──●──●──●──▶   통합·검증(3101/9001, kei-dev-0703 워크트리). 항상 배포 가능 상태 유지
      ├ feat/<기능명>          기능 개발(예: feat/deadlines, feat/reader-glass)
      ├ fix/<이슈>             버그 수정
      └ chore/<작업>           설정·정비
```

- **main** = prod 그 자체. 직접 커밋 금지 — `dev → main` 병합(승격)으로만 갱신. 병합 시 태그.
- **dev** = 3101에서 항상 굴러가는 통합 브랜치. 기능 브랜치의 병합 대상.
- **기능 브랜치** = `dev`에서 분기 → 개발·검증 → `dev`로 병합(가급적 `--no-ff`로 이력 보존) → 삭제.
  이름: `feat/`·`fix/`·`chore/` + 짧은 영문 케밥(예: `feat/quality-board`).
- 워크트리 운용: `/KEIAdminSuperv`=main 고정, `kei-dev-0703`=dev(기능 브랜치 작업 시 그 안에서 checkout).
- **push**: dev는 수시 push(백업·공유), main은 승격 시 push. SSH(`git@github.com:mooner92/…`)만
  ([[git-push-auth]] — HTTPS 자격증명 없음). ⛔ 볼트·rule_files 등 내부 데이터는 계속 gitignore
  ([[data-separation-security]]).

## 2. 커밋 규칙 (Conventional Commits 변형)

형식: `<type>(<scope>)?: <제목>` — 제목은 한국어 OK. 본문·푸터 자유.

| type | 용도 |
|---|---|
| `feat` | 새 기능 |
| `fix` | 버그 수정 |
| `docs` | 문서만 |
| `refactor` | 동작 불변 구조 개선 |
| `perf` | 성능 |
| `test` | 테스트·검증 스크립트 |
| `chore` | 설정·의존성·잡무 |
| `build` / `ci` | 빌드·CI |
| `style` | 포맷(로직 불변) |
| `revert` | 되돌림 |
| `polish` / `refine` | UI·UX 다듬기(이 레포 관례 유지) |
| `harden` | 보안 강화(〃) |

- 강제: `.githooks/commit-msg` 훅(위반 시 커밋 거부). `git config core.hooksPath .githooks` 필요
  (pre-commit gitleaks와 동일 경로 — 이미 설정돼 있음).
- scope는 선택(예: `feat(deadlines):`), 파괴적 변경은 `!`(`feat!:`) + 본문에 설명.
- Merge/Revert/fixup 자동 메시지는 훅이 통과시킴.

## 3. 버전 (v1.x.x)

- **MAJOR(1)**: 서비스 정체성 수준 개편(당분간 1 고정).
- **MINOR(x)**: `dev → main` 승격마다 +1 — 기능 묶음 릴리스. 승격 커밋에 태그 `v1.X.0`.
- **PATCH(x)**: 승격 사이 main에 직접 들어가는 긴급 수정(hotfix)만 +1(`v1.X.Y`).
- 태그는 annotated: `git tag -a v1.2.0 -m "KRDS 전면 개편 + …"` → `git push origin --tags`.
- ⚠ 태그는 **코드만** 스냅샷 — 실제 prod 복원은 태그 + 데이터(볼트·chroma·app.db) 백업 세트
  ([[dev-prod-two-servers]] 승격 절차 참조).

### 이력
| 태그 | 시점 | 내용 |
|---|---|---|
| v1.0.0 | 2026-07 초 | 최초 프로덕션(TDS) |
| v1.1.0 | 2026-07-05 | Qwen3.5-9B + 별표 graph-expand |
| v1.2.0 | 2026-07-22 | KRDS 전면 전환 + prod 승격(main 컷오버) — df776f7 |
| v1.3.0 | 2026-07-23 | 상위법령 레이어 + kordoc 전량 재변환(84/111) + 자가평가 자동화 + 절차팩 — 6ab5449 |
| v1.4.0 | 2026-07-24 | 호롱(Horong) 디자인 리뉴얼 + 답변 품질 자정 루프(규칙17·골든 회귀) + 즉시 가입 + 대외업무 캘린더 재구축 — d9a9a6b |
| v1.5.0 | 2026-07-25 | 답변 품질 자정(거부판정기 T9·정의형 라우팅·검색라벨 v2) + 규정 용어 사전 194종·상위법령 별표 74종 + 컴포넌트 전수 통일 — edbb529 ⚠재색인(EMBED_CTX_LABEL=1)+01z 필요 |
| v1.5.1 | 2026-07-25 | 서식 PDF 미리보기 사이드패널(공용 SideDrawer) + 품질 추이 분리(전체 이력 페이지) + 색인제외 철회·재색인 안전성 검증 — 2053b0f ⚠재색인(EMBED_CTX_LABEL=1)→kei_regs_v4 |
| v1.6.0 | 2026-07-26 | 개정 영향 분석(조문 660 파급·/impact·챗봇 라우팅) + 금액 전결 판정(별표 8업무·24구간, LLM 무판정) + 서식 미리보기 프레이밍 픽스 + 크론 알림 — ee6af94 ⚠재색인 불필요·tools/index 재생성(01l·01r2) |
| v1.7.0 | 2026-07-27 | 자가평가 축 4종·복합 시나리오·거부형 채점 교정(specs/07) + 규칙17(비용≠이용가부) + 디자인 정본화 — b7427a4 |
| v1.7.1 | 2026-07-27 | 브랜드 이야기 /brand(flag brand_page on) — e63f6a9 |
| v1.7.2 | 2026-07-27 | 문서 탭 스크롤 픽스(BrowseShell 이관) + 워딩 규정→문서 — ed4c3d9 |
| v1.8.0 | 2026-07-28 | **호롱 v2 Spotify 리디자인**(다크 기본·사이드바 2패널 셸·온디맨드 근거·트랙리스트) + 결재선 01n HTML 파서 수술(금액 룰 10업무·32구간) + 용어 팝오버 수정 — 918598f ⚠재색인 불필요·tools/index(approval·amount) 이관 |
| v1.8.1 | 2026-07-29 | **보안 스캔 22건 대응**(로그인 게이트 dot-segment 우회 차단·경로 제어문자 크래시·인증 3종·내부콘텐츠 가드 fail-open·오토픽스 부분격리) + dev 테스트계정 14개 비밀번호 회전 — docs/63 ⚠재색인 불필요·웹 재빌드 필요 |
| v1.9.0 | 2026-07-31 | 운영 알림 카탈로그 8종(Slack `#horong`·유출 백스톱) + 코드 지식그래프(graphify) + 실험실 `/lab` + 출제 3중 필터 — ⚠dev chroma 전체 prod rsync + `RAG_COLLECTION=kei_regs` 전환 |
| v1.10.0 | 2026-08-03 | 어휘 갭 평가(문서어/일상어 짝 대조) + 아침 분석서(수술대기/측정노이즈 분리) + 여정 신선도 감시 + 카드 시인성 — d2ee141 |
| v1.11.0 | 2026-08-10 | 개정 반영(신·구조문 대비표 판별·한 줄 전사) + Wave 1 측정 무결성(거부형 91건 복구·거부오염 수리·게이트 계측) + 해상도 수정 — 7c977f5 ⚠재색인(EMBED_CTX_LABEL=1) 포함 |
| v1.12.0 | 2026-08-14 | **여비 계산기**(실사용 질문 1위) + 여정 4종(자문수당·예산전용·위촉연구원·초과근무 분기) + 개정 이력 시각화 L1~L3 + 거부 트리거 재검색 + BM25 하이브리드 + 전결 tie-break + 재색인 파생 인덱스 자동화 + **볼트 604문서 검수완료 전환** — ⚠볼트 rsync 필수(검수상태·여정), 플래그 `travel_calc`·`journey_freshness`·`corpus_amend` on |

## 4. 승격(릴리스) 절차 요약
1. dev에서 검증 완료(3101 실렌더·평가) → 사용자 승인
2. `main`에 `dev` 병합(--no-ff) → 태그 `v1.X.0` → push(main+tags)
3. prod 데이터 이관·재빌드·재시작(승격 런북: [[dev-prod-two-servers]] gotcha —
   ① PM2 env는 delete+start ② flag 테이블 복제 ③ **APP_DB env 필수**(ecosystem.local.js —
   없으면 rag_core가 플래그를 못 읽어 uplaw·절차팩 등 백엔드 플래그 기능 전부 미발동,
   v1.3.0 승격 실측) ④ 볼트·chroma·tools/index·forms-pdf·quality rsync(백업 선행)
   ⑤ 신규 npm 의존성은 npm install 먼저)
4. **⚠ 백머지(필수) — `main → dev`**: 승격 때 main에서만 생기는 커밋(병합 커밋 + docs/60 이력·
   prod 설정)이 dev로 돌아오지 않으면 두 브랜치가 갈라진다("main is ahead" 표시의 정체).
   ```bash
   git checkout dev && git merge origin/main --no-edit && git push origin dev
   git log --oneline dev..origin/main | wc -l   # 0이어야 정상
   ```
   실측(2026-07-26): v1.4.0~v1.5.1 3회 승격 동안 백머지를 안 해 main에만 6커밋이 쌓였고,
   사용자가 "main이 dev보다 앞서 있다"고 발견. 서비스 영향은 없었지만 이후 분기 브랜치가
   승격 이력 없이 시작되는 혼란이 생긴다.
5. dev는 계속 다음 작업

## 5. 예외·주의
- 훅 우회(`--no-verify`)는 긴급시에만, 사후 정리 필수.
- 기능 브랜치 장기 방치 금지 — dev와 자주 리베이스/병합.
- `feat/0620`(구 prod)·`feat/krds`·`feat/0703`·`feat/0722`는 **전환기 이력 브랜치** — 보존만,
  신규 작업 금지(신규는 반드시 dev에서 분기).

# docs/38 — 아이디어 풀 (디자인·내용, 4관점 패널 도출 2026-07-15)

> 4관점(신입 UX·콘텐츠 데이터·운영 루프·채팅 상호작용) 병렬 아이디에이션 결과를 큐레이션.
> 전 항목 공통 제약: 기존 데이터만 사용(창작 0)·가드레일 불변·기능 플래그·정적 export·무LLM 우선.
> ★ = 두 관점 이상이 독립적으로 수렴한 아이디어(신호 강함).

## A. 빠른 승부 (S — 각 반나절 이내)

| 아이디어 | 측면 | 요지 | 데이터원 |
|---|---|---|---|
| **상황 시작 칩** | 둘다 | 채팅 빈 화면에 "첫 출장을 가요/연차를 쓰고 싶어요" 상황 칩 → 여정 딥링크+서식+추천 질문 프리필 | _journeys 9종(기존) |
| **부서 문의 핸드오프 카드** ★ | 둘다 | 거부 답변 아래 '담당 부서에 물을 준비' — 내 질문+확인한 근거 조문+기준일을 복사용 카드로 조립 | 메시지 저장 근거(기존) |
| **기한 사전(/deadlines)** | 내용 | 전 규정 기한 228건 역방향 브라우저(사건→규정) + 기존 기한 계산·.ics 인라인 | deadlines.json(01m) |
| **대화 근거 장부** | 내용 | 근거 패널에 '이 대화에서 인용된 조문' 누적 칩 + 대화 전체 복사(기안 첨부용) | messages[].sources(기존) |
| **k-익명 인기 질문 시작 칩** | 내용 | 관리자만 보던 인기 질문(K명 이상)을 신입 빈 화면 시작 칩으로 | /app/stats k_anon 재사용 |
| **검색어 재작성 투명화** | 내용 | 멀티턴 재작성 검색어를 "🔍 이렇게 검색했어요"로 표시 + '주제가 달라요' 1클릭 재검색 | condense_query 출력(기존) |
| **수요×품질 사분면 차트** | 디자인 | 신뢰 탭 매트릭스를 CSS 산점도(인용수×👎×검수상태)로 — 검수 ROI 지도 | /app/trust(기존) |
| **알려진 주의사항 게시판** | 둘다 | 사람 판정 완료된 정합성 감사 결과(낡은 값)를 도움말·해당 문서 드로어 배너로 공개 | conflict_audit.json(판정분만) |

## B. 중간 (M)

| 아이디어 | 측면 | 요지 |
|---|---|---|
| **용어 인라인 툴팁/정의어 밑줄** ★★ | 둘다 | 답변·본문 속 행정 용어(복명·전결·품의) 점선 밑줄 → 용어집/규정 원문 정의 팝오버(0클릭·무LLM). 두 관점이 독립 수렴 — 최우선 후보 |
| **ERP 길찾기** | 내용 | '그 메뉴 어디예요?' — 40_시스템 53노트의 화면코드·경로를 /forms 패턴으로 검색 |
| **첫 2주 지도(/start)** | 둘다 | Day1/첫주/둘째주 온보딩 트랙 — 여정·서식·용어를 시간축으로 꿰는 진입점(진행률 localStorage) |
| **규정 연대기(/timeline)** | 둘다 | 조문 개정 시계열(1,912조문·신설116·삭제50) 타임라인 + 자주 바뀌는 조문 |
| **서식-조문 역링크** | 내용 | 서식↔요구 조문 1클릭 왕복(clause_xref byeol 엣지 336) |
| **내 서랍(북마크)** | 디자인 | 조문·서식·여정 로컬 북마크+최근 본 문서(참조만 저장 — 옛 규정 고착 방지) |
| **답변 해부 레이아웃** | 디자인 | 핵심답 콜아웃·절차 스테퍼·계산식 블록 — 생성 텍스트 재배치만(문구 불변) |
| **인라인 인용 칩** | 둘다 | 본문 속 [규정명 제N조]를 칩+호버 미리보기로(각주→하이퍼텍스트) |
| **첫 정산 프리플라이트** | 둘다 | 복귀일 입력 → 마감 D-day+.ics+증빙 목록+ERP 화면 한 장(여정 JSON 조립) |
| **코퍼스 건강 위젯** | 둘다 | /now·도움말에 검수 진행률 링·최근 검수 5건 — "관리되고 있다" 신호 |
| **ops_digest.py** | 내용 | 주간 운영 브리핑 1페이지 자동 생성(검수 큐+피드백+사용량+가입 대기) |
| **그래프 파급 모드** | 디자인 | 노드 선택 시 개정 파급 범위를 hop색으로 + 근거 패널→그래프 딥링크 |
| **정적 타이프어헤드** | 내용 | 입력창 자동완성(규정명·용어·서식·여정 — 실존 제목만, 무LLM) |
| **수치 스토어 미니 표** | 디자인 | 검수완료 표를 진짜 `<table>`+조건 행 하이라이트(⏳검수 진행 후 빛남) |

## C. 큰 것 (L)

- **/admin 검수 큐 탭** — CLI 큐를 화면 패킷으로(점수 분해·👎 사유·충돌 링크). ⛔ 확정 버튼 없음(검수상태 변경은 계속 사람·CLI만)

## 추천 우선순위(사견)

1. **용어 인라인 툴팁**(★★ 수렴 + 신입 가치 최대) → 2. **상황 시작 칩**(S, 빈 화면 문제 직격)
→ 3. **부서 문의 핸드오프 카드**(S, 거부 UX 완성) → 4. **기한 사전**(S, 기존 인덱스 개봉)
→ 5. **답변 해부 레이아웃**(디자인 체감 큰 것). 단, docs/37(KRDS 전환)이 시한성 있어 그쪽 먼저.

# docs/61 — 상위 법령 레이어 (설계안, 2026-07-22)

> 상태: **설계만 · 구현 미착수**(사용자 승인 후 착수). 코드 변경 없음.
> 배경 조사: NRC/공공 규정 코퍼스(이전 턴), `korean-law-mcp` 등 MCP 저장소 비교(이 턴).
> 관련: [[data-separation-security]] · CLAUDE.md 절대규칙 1·3·5 · `50_대외업무`의 "(운영 통계)" 라벨 선례.

## 0. 한 줄 정의
KEI 사내 규정에 **없는** 질문(휴가 기안·연구비 카드 조건 등)에, 사내 규정의 **상위 규범**
(연구개발비 고시·혁신법·복무예규 등)을 **별도 레이어**로 두고, 거부(“규정에서 확인되지 않습니다”)를
**약화시키지 않은 채 그 뒤에** `(상위 법령)` 라벨로 참고를 덧붙인다.

## 1. 왜 — 문제와 목표
- 현상: 사내 규정에 항목이 없으면 챗봇은 "확인되지 않습니다"로 끝난다(절대규칙대로, 옳음).
  하지만 그 답의 상당수는 **상위 법령에는 명확히 존재**한다(연구비 카드 = 「국가연구개발사업
  연구개발비 사용 기준」 고시, 휴가·복무 = 인사혁신처 예규).
- 목표: **빈손 거부의 UX 약점만 보완**하되, 사내 규정과 일반 법령의 경계를 항상 사용자에게
  보이게 유지(절대규칙 1·3 불변). 사내 근거가 있으면 그게 정본, 상위 법령은 **보조**로만.

## 2. 왜 MCP를 쓰지 않는가 (조사 결론)
`korean-law-mcp`(chrisryugj, TS/Node, MIT)·`mcp-kr-legislation`(ChangooLee, **Python/FastMCP**, MIT)·
`seo-jinseok/korean-law-mcp`(Python) 모두 결국 **law.go.kr(법제처) DRF Open API를 감싼 래퍼**다.
MCP는 "런타임에 에이전트가 도구를 실시간 호출"하는 모델이라 우리 구조와 **세 가지가 어긋난다**:

1. **오프라인 원칙 위반**(절대규칙 5) — 질문마다 외부 정부 API를 때리면 사내망 격리가 깨지고,
   API 점검·장애 시 답변이 멈춘다.
2. **의미 검색 우회** — API는 "법령 문서 하나"를 줄 뿐, 그 안 **어느 조문이 근거인지**는 못 찾는다.
   그 일을 하는 게 우리 임베딩(KURE-v1)+리랭커(bge-v2-m3)다. 실시간 호출은 이 파이프라인을 건너뛴다.
3. **로컬 Qwen 함수콜 한계** — MCP 클라이언트가 붙으려면 안정적 tool-calling이 필요한데
   Qwen3.5-9B로는 신뢰도가 낮고, 우리 답변 파이프라인은 애초에 "검색→주입→생성"(툴콜 아님)이다.

⇒ **MCP 서버는 도입하지 않는다.** 대신 위 저장소(특히 Python인 `mcp-kr-legislation`이
레포에 **법제처 API 173개 JSON 메타 + 크롤러**를 포함)의 **API 매핑·파라미터·별표 추출 로직만
설계 참고**로 쓰고, 수집은 우리 파이프라인 도구(아래 §4)로 흡수한다.

## 3. 왜 cron 미러링인가 (용량·속도 검토 — 사용자 논의)
- **용량**: 전체 법령 DB(수십만 건)가 아니라 **KEI에 실제 적용되는 상위 규범 allowlist 30~50건**만
  받는다. 전부 텍스트 → **총 10~30MB**(우리 볼트와 동급). "엄청 크다"는 전체 미러링 가정일 때 얘기.
- **속도(호출식)**: law.go.kr 호출당 수백ms~1s로 "못 쓸" 정돈 아니지만, 위 §2의 오프라인·의미검색·
  함수콜 문제가 속도보다 크다. → 런타임 호출 기각.
- **cron 증분**: 매일 allowlist의 **시행일자 메타만 싸게 조회 → 우리 버전과 diff → 바뀐 것만 본문
  재수집·재임베딩**. 평상시 "변경 없음"으로 수 초 종료. 우리 골든 `--sync`·코퍼스 재색인과 동형.

## 4. 구조 (빌드타임 수집 → 별도 레이어 → 라벨 회수)

### 4-1. 볼트 레이어
- 신규 폴더 **`25_상위법령/`** (20_규정원문과 병렬, 그러나 **사내 규정 아님**을 폴더·라벨로 구분).
  분류 예: `연구개발비고시`·`복무예규`·`혁신법령`·`NRC공통규정`.
- 프론트매터: `type: uplaw`(신규 신분), `법령명`, `공포일자`, `시행일자`, `소관부처`, `출처URL`,
  `검수상태: 미검수`, `적용강도(직접|준거|참고)`. ⛔ 원문 의역 금지(절대규칙 2 동일 적용).

### 4-2. 수집 스크립트 `tools/01h_law_fetch.py` (가칭)
- law.go.kr **DRF Open API 직접 호출**(MCP 없음):
  - 목록: `http://www.law.go.kr/DRF/lawSearch.do?OC=<키>&target=<법령종류>&type=XML&query=…`
  - 본문: `http://www.law.go.kr/DRF/lawService.do?OC=<키>&target=…&type=XML&…`
  - `target` 확인 필요: 법령=`law`/`eflaw`, **행정규칙(고시·훈령·예규)=`admrul`**, 해석례=`expc`.
    (고시·예규가 실제 어느 target으로 나오는지는 OC 키 발급 후 실호출로 검증 — §6 미검증 항목)
- **인증**: 법제처 OC 키(무료, `open.law.go.kr/LSO/openApi/guideList.do`). env `LAW_OC`.
  사내망 대비 `LAW_API_PROTOCOL=http`·`LAW_REFERER`(law.go.kr은 Referer 요구) 참고(korean-law-mcp env에서 확인).
- 변환: XML → 조문 단위 마크다운(우리 `제N조` 구조 유지) → `25_상위법령/`. 별표·서식은 기존
  별지 파이프라인(`01p_byeolji_pdf.py`) 재사용 가능.
- **증분**: allowlist(`tools/law_allowlist.json`)의 각 항목 `시행일자`를 조회해 저장분과 diff,
  변경분만 재수집. 산출 로그로 "갱신 N건 / 무변경 M건" 표기.

### 4-3. 임베딩
- **별도 Chroma 컬렉션**(예: `kei_uplaw`) — 사내 규정(`kei_regs`)과 물리 분리. 재색인 독립.
- 청킹은 규정원문과 동일(제N조 단위).

### 4-4. 회수·답변 (라벨 게이트)
- `rag_core.retrieve`가 사내(`kei_regs`) 회수 결과가 **비거나 신뢰도 낮을 때만** `kei_uplaw`를
  보조로 조회(플래그 `RAG_UPLAW_LAYER`, 기본 off). 사내 근거가 있으면 상위 레이어는 첨부 안 함.
- 근거 블록에 **`(상위 법령 — 사내 규정 아님, 적용강도: 직접|준거|참고)`** 라벨(‘운영 통계’ 라벨과
  동형, `rag_core.SYSTEM` 신규 규칙으로 처리).
- **SYSTEM 신규 규칙(초안, 규칙15 예정)**: "[근거]에 '(상위 법령)' 라벨이 붙은 블록은 KEI 사내
  규정이 **아니라 상위 법령**이다. ⓐ 먼저 '사내 규정에서는 확인되지 않습니다'를 유지하고, ⓑ 그
  뒤에 '일반적으로 상위 법령(<법령명>)은 …'으로 **구분해** 안내한다. ⓒ 적용강도가 '참고'(예:
  공무원 복무예규)면 'KEI에 직접 적용되는지는 규정·담당 부서 확인이 필요합니다'를 반드시 붙인다.
  ⓓ 수치·한도는 그 상위 법령 원문에 있을 때만 인용(기존 수치 게이트 동일 적용)."
- UI: 근거 패널에 **📜 상위 법령** 칩(기존 source_type_badges 게이트 재사용).

## 5. 절대규칙 정합성 체크
| 규칙 | 준수 방법 |
|---|---|
| 1 지어내기 금지 | 상위 법령도 **원문 인용 + 출처URL**만. 수치는 원문에 있을 때만(수치 게이트 적용). |
| 2 원문 의역 금지 | `25_상위법령/`도 원문 보존, 조 구조 유지. |
| 3 출처 표기 | `[법령명 제N조]` + 출처URL. `(상위 법령)` 라벨로 사내와 구분. |
| 4 가드레일 불변 | 거부 문구를 **덮어쓰지 않고 뒤에 덧붙이는** 구조 — 거부는 사내 규정 층에만 적용, 유지. |
| 5 인터넷 미공개 | 수집은 **빌드타임만** 인터넷 접촉. 런타임은 오프라인 임베딩만 사용. |

## 6. 검증 현황
### ✅ U1 실호출 검증 완료 (2026-07-23, OC=mooner92)
- **인증·도달성**: `lawSearch.do?OC=mooner92` → `resultCode 00 success`. egress IP `203.250.98.129`
  (폼 '서버장비 IP'에 등록 권장 — OC 인증이라 없어도 대개 동작하나 안전).
- **allowlist 전건 조회 성공**(현행법령 target=`law`): 근로기준법(고용노동부)·국가연구개발혁신법·
  공공기관의 운영에 관한 법률·개인정보 보호법·환경영향평가법 모두 `현행` 매칭.
- **KEI 설립 모법 확정**: `정부출연연구기관 등의 설립·운영 및 육성에 관한 법률`(+시행령) —
  ⚠ **"과학기술분야"가 붙은 변종이 아니다**(KEI=경제·인문사회 계열, NRC 소속). 둘 다 존재하니
  수집 시 정확한 법령ID로 지정할 것.
- **행정규칙(target=`admrul`) 조회 성공**: 국가연구개발사업 연구개발비 사용 기준(cnt=1)·환경영향평가서
  등에 관한 협의업무 처리규정(cnt=1).
- **본문 조문 회수 작동**(`lawService.do?target=law&ID=001872`): 근로기준법 261KB·**145개 조문**,
  `제60조(연차 유급휴가)` 텍스트 정확 추출 → **우리 제N조 청킹에 그대로 투입 가능** 확인.

### ⏳ 남은 확인
1. **이용약관·재배포**: 신청 페이지 약관으로 상당 해소 — "공공데이터 정책상 자유 활용 보장 +
   **출처(법제처) 표기 의무** + **위조·변조 금지** + 법제처 이외 기관 명기 금지". 우리 `(상위 법령 —
   출처: 법제처)` 라벨 + 원문 의역 금지(절대규칙 2)로 준수. (온프레미스 임베딩=참고 활용 범위로 판단,
   대량·상시 호출 시 사전 협의 문구만 유의 → cron 저빈도라 문제 없음.)
2. ✅ NRC 공통 규정(`rule.nrc.re.kr`) — **공개 확인(로그인 불필요, 2026-07-23)**. Open API는 없고
   각 규정 전문(HWP)·PDF 다운로드 제공 → §7-B 참조.

## 7. allowlist (KEI 규정 원문 대조로 확정, 2026-07-23)
**⚠ 부처별로 고르는 게 아니다** — law.go.kr의 "대한민국 현행법령"·"현행 행정규칙"은 전 부처 통합
코퍼스(법령명으로 조회). 폼의 "○○부 법령해석"만 부처별 데이터셋(해석 사례)이고, 이는 스코프 밖.
아래 allowlist는 **볼트 규정 원문에 실제 인용된 상위법을 grep 대조**해 확정(빈도=인용 파일 수).

| 법령 | target(추정) | 적용강도 | 근거(KEI 규정 내 빈도·인용) |
|---|---|---|---|
| **근로기준법 + 시행령** | law/eflaw | **직접(최우선)** | 11개 — "직원은 근로기준법 제60조에 따라 연차유급휴가"·제55조 휴일·제76조의2 괴롭힘. KEI 직원=근로자 |
| **정부출연연구기관 등의 설립·운영 및 육성에 관한 법률 + 시행령** | law/eflaw | **직접(설립 모법)** | KEI 존립 근거법(1110 파일) |
| 국가연구개발혁신법 + 시행령 + 시행규칙 | law/eflaw | 직접 | 연구관리 모법 |
| 국가연구개발사업 연구개발비 사용 기준(과기정통부 고시) | admrul | 직접 | 연구비 카드·집행 |
| 공공기관의 운영에 관한 법률 + 기재부 지침 | law/admrul | 직접(운영층) | 기획재정부 5개·공공기관운영 1개 |
| 개인정보 보호법 + 시행령 | law | 직접 | 4개(보안·정보 규칙) |
| 국가재정법 · 국가를 당사자로 하는 계약법(조달) · 보조금법 | law | 직접(예산·계약) | 조달청 5개·국가재정법 1개·보조금 3개 |
| 환경영향평가법 + **환경부 예규·고시**(평가서 검토) | law/admrul | 직접(수탁 핵심업무) | 환경부 10개 — 환경평가서 검토는 환경부장관 의뢰 수탁업무 |
| NRC(경제·인문사회연구회) 공통 규정 | (별도 경로 §7-B) | 준거 | 연구회 30개·NRC 5개(모법/준거) |
| 국가공무원법 · 국가공무원 복무규정 · 복무예규(인사혁신처) | law/admrul | **참고(직접 적용 아님)** | 5개 — 준용만. ⛔ 휴가·복무는 근로기준법이 우선 |

> ✅ **불필요 확인**(0건): 산림청·기상청·국가데이터처 등. KEI 업무 무관 → allowlist 제외.
> 🔧 **초안 대비 교정**: 휴가·복무의 직접 상위법은 복무예규가 아니라 **근로기준법**(직원=근로자).
>   "휴가 며칠?"에 공무원 기준(연 21일 등) 인용은 오답 — 근로기준법 제60조가 정답.
>   설립 모법(출연연 육성법)·환경평가 환경부 예규·개인정보보호법·예산계약법도 대조로 신규 추가.

### 7-B. NRC 공통 규정 allowlist (rule.nrc.re.kr '공통지침 등' 대조, 2026-07-23)
NRC 시스템은 커 보이나(정관 25건·평가결과 등), **대부분 타 24개 연구기관 문서**라 KEI 무관.
KEI에 적용되는 **공통규범은 6개뿐**(볼트 대조: KEI는 자체 하위 규정만 보유 → NRC 상위본은 신규):

| NRC 문서 | 적용강도 | 볼트 현황 |
|---|---|---|
| **연구회 및 연구기관 운영 표준지침** | 준거(최우선) | ❌ 볼트 부재 — 순신규 |
| 연구회 및 연구기관 예산운용지침(2022~2026) | 준거(예산) | KEI 자체 예산규정만 |
| 소관 연구기관 연구개발적립금 관리규정 | 준거(적립금) | KEI 자체본만 |
| 기본연구사업 관리 및 운영규정 | 준거 | KEI 자체본만 |
| 경제·인문사회연구회 연구윤리 규정 | 준거 | KEI 자체 연구윤리규정만(상위 구분 라벨) |
| 경제·인문사회연구회 정관(모기관) | 준거 | KEI 자체 정관만(상위 구분) |

> ❌ 제외: 23개 타 기관 정관(KEI 정관은 이미 보유)·평가결과 보고서(실적, 규정 아님).
> ⚠ 선택(후속): 연구기관 평가편람·감사사례집(규정 아닌 참고자료).

**수집 방법(law.go.kr과 다름)**: NRC는 **Open API 없음** — 각 규정에 전문(HWP)·PDF 다운로드 제공.
6개·저빈도 개정(2015~2025 몇 년 단위)이라 **크롤러·cron 불필요**. HWP 6개 다운로드 →
**기존 `01_hwp_to_md` 파이프라인 재사용** → `25_상위법령/NRC/`. 개정 시 사람이 재다운로드
(⟳ 재색인 필요 마킹). URL 패턴: `rule.nrc.re.kr/lmxsrv/law/lawDetail_areaC.do?SEQ=<n>&LAWGROUP=2`.

## 8. 단계 (승인 후)
- **U1** ✅(2026-07-23) OC 키 발급 + target 실호출 검증 완료(§6) — 인증·전건 조회·본문 조문 회수 확인.
- **U2-ⓑ/K2** ✅(2026-07-23) NRC 6종 볼트 적재 완료 — `25_상위법령/NRC/` (type: uplaw·미검수).
  수집은 01_hwp_to_md 대신 **kordoc(v4.2.7)+어댑터**로 확정(표 병합 셀 보존 — K1 실측 우세):
  kordoc → `kordoc_adapt --check --parity`(5종 ✅) → 대형 2종은 가짜 경계 10곳 결정적 결합
  (내용 불변 검증) 후 표준지침 180=180 일치·예산운용지침은 kordoc이 현행 놓친 조문 1건을
  더 살림(제3조(적립금의 범위) — 수동 승인, docs/62 T5) → `01y_uplaw_ingest.py` 적재.
  격리 확인: 02 메인 색인 5,507청크 중 25_상위법령 혼입 0(디렉토리 스킵) · 웹 SECTIONS ·
  관리자 목록(type 필터) 자동 제외. KEI 정관은 볼트본(2021-08-19)과 동일 개정이라 적재 제외.
- **U2-ⓐ** ✅(2026-07-23) `01h_law_fetch.py` + `law_allowlist.json` — law.go.kr Open API 16건
  수집(근로기준법+시행령·출연연법+시행령·혁신법+시행령·연구개발비고시·공공기관운영법·개인정보
  보호법·재정법·계약법·보조금법·환경영향평가법+협의업무규정·공무원법+복무규정[참고]).
  정확 일치만 채택(유사명 추측 금지 — '과학기술분야' 변종 배제 실증), 증분 상태
  (law_fetch_state.json — 재실행 시 16 skip 실측), 개인정보 필드(담당자명·전화) 미기록,
  조문·부칙만(별표 첨부 v1 미수집·비고 명기). → 25_상위법령/법령/ 16노트(~835K자).
  kei_uplaw 재임베딩 588→**4,116청크**. E2E: "1년 미만 연차" → 사내 복무규정 제16조 정본
  + ⚖근로기준법 제60조(직접) 보조 첨부·구분 인용 ✅.
- **U3** ✅(2026-07-23) `kei_uplaw` 컬렉션 임베딩 — 02 `--layer uplaw`(25_상위법령만 스캔·조 단위
  청킹·적용강도 메타). NRC 6종 → **588청크**. law.go.kr allowlist 확장·cron 증분은 U2-ⓐ와 함께 후속.
- **U4** ✅(2026-07-23) rag_core 보조 회수 — `_uplaw_on()`(env RAG_UPLAW_LAYER > flag `uplaw_layer`,
  기본 off) + kei_uplaw top-2(거리 임계 RAG_UPLAW_MAX_DIST=0.55, 무관 첨부 차단) →
  '(상위 법령 — 사내 규정 아님 · 적용강도 · 출처)' 라벨 블록을 사내 근거 **뒤에** 첨부.
  SYSTEM 규칙15(사내 정본 우선·부재 시 "사내 확인 안 됨 → 다만 상위 규범…" 구분·참고 강도는
  부서 확인 문구·⛔사내 규정 사칭 금지). UI = ⚖ 상위 법령 칩(항상 표시 — 층위 구분은 필수 정보).
  E2E: 적립금 적정규모 질의 → uplaw 2건 첨부·정확 출처 인용 ✅ · flag off 회귀 0 ✅.
- **U5** 골든셋에 상위법령 문항 추가 → 지표 불변(사내 답변 회귀 없음) 검증.

## 9. 둘러보기·그래프 노출 (2026-07-23)
- **둘러보기**: SECTIONS에 `상위법령`(dir 25_상위법령, 라벨 "상위 법령(참고)", 슬레이트 accent).
  분류 필터 = 소관부처(vault.ts category 폴백). 문서 페이지 = ⚖ 적용강도 태그 + 상단
  "사내 규정 아님" 콜아웃. 드로어·검색(내용검색)·docdata 자동 합류(412문서).
- **그래프**: 01b 레지스트리에 uplaw 법령명 추가 — 사내 규정 원문의 법령 인용(「근로기준법」
  제60조 등)이 [[링크]] 엣지가 됨(**92링크·42파일**, 방향 = 사내 규정 → 상위 법령).
  멘션 스캔은 계속 20_만 — 상위법령 원문 무수정. 노드 색 = 슬레이트(#64748b/#94a3b8).
  실측: 근로기준법 백링크 16건(인사·복무규정 등). Playwright 7/7.
- **서식**: law.go.kr 별표·서식 첨부파일은 v1 미수집 — /forms 연동은 별표 수집(v2)과 함께 후속.
- **수치(2026-07-23)**: 볼트 412문서(규정 111·가이드 65·용어 135·시스템 65·대외 14·상위법령 22)
  · kei_regs 5,578청크 · kei_uplaw 4,116청크.

## 참고 저장소(설계 참고용 — 도입 아님)
- chrisryugj/korean-law-mcp (TS, MIT) — 도구 10종·별표추출·인용검증·사내망 env 참고.
- **ChangooLee/mcp-kr-legislation** (Python/FastMCP, MIT) — **173 API JSON 메타 + 크롤러**, 최상위 코드 참고.
- seo-jinseok/korean-law-mcp (Python, MIT) — 간결한 DRF 매핑.
- 법제처 Open API 가이드/OC 발급: https://open.law.go.kr/LSO/openApi/guideList.do

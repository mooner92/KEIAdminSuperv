# docs/46 — 랜딩(/about) 시네마틱 타이포 개편

> 2026-07-16. 참고: chris.gomdori.app 분석(초대형 타이포·그라디언트 한 줄·모노 아이브로·수치 스트립).
> 지시: **비슷하게 베끼지 말 것** — Apple·Toss·SpaceX를 배합해 "진화된 깔끔함".
> 대상은 /about(full variant)만. 비로그인 '/'(home variant)·다른 페이지는 불변.

## 1. 컨셉 — "정제된 시네마틱"
| 원천 | 가져오는 것 | 버리는 것 |
|---|---|---|
| gomdori | 초대형 헤드라인 뼈대 · 모노 아이브로 · 수치 스트립 | 무지개 그라디언트 · 검정 단일 배경 · 과밀 정보 |
| Apple | 자간 -4.5% 타이트 타이포 · 섹션 여백 확대 · 절제된 리빌(살짝 상승+페이드) | — |
| Toss | 존댓말 친근 카피(기존 유지) · 큰 필 CTA · 부드러운 카드 | 마스코트류 장식 |
| SpaceX | 시네마 다크 히어로 · 모노 대문자 라벨 · 숫자 우선 · SCROLL 큐 | 실사 배경 이미지 |

**그라디언트 규율**: 전체에서 딱 한 곳 — 히어로 강조 한 줄. KEI 블루→퍼플→핑크 3정지(주황 금지).
폰트는 기존 Pretendard GOV 그대로(추가 로드 0) — 대형에서 자간만 조이면 충분(실측: gomdori도 Pretendard).

## 2. 변경 목록 (전부 CSS+카피, 구조 불변 — 섹션 6개·ScrollRail·플래그 유지)
1. **히어로**: kicker(mono 소형) → 2행 초대형 헤드라인
   `물어보면,` / `규정이 답합니다.`(← 유일 그라디언트) — clamp(52px, 8.5vw, 104px)·weight 800·자간 -0.045em·행간 1.04.
   서브리드에 기존 필수 문구 유지("모든 답변에 [규정명 제N조] **출처가 달립니다**" — verify 계약).
2. **메타 스트립**(히어로 하단, SpaceX): mono 대문자 라벨+숫자 —
   `규정 원문 {regs} · 업무 가이드 {guides} · 행정 용어 {terms} · 출처 표기 100%`(counts 실측치, 0이면 숨김).
3. **SCROLL 큐**: 히어로 맨 아래 mono `SCROLL ↓` + 은은한 바운스(reduced-motion 시 정지).
4. **넘버링 아이브로**: `01 — 질문하기` … `05 — 시작하기`(mono·tabular-nums, 액센트 색 유지).
5. **수치 나열형 헤드라인**(explore): "규정 {regs} · 가이드 {guides} · 용어 {terms} — 전부 연결돼 있습니다."
6. **타이포 스케일 상향**: 섹션 h2 clamp(32px, 4.2vw, 50px)·자간 -0.03em, 섹션 세로 패딩 96→128px(데스크톱).
7. **리빌 모션**: opacity+translateY(12px), 0.5s ease, 자식 지연 계단(60ms) — 기존 IO 훅 재사용, reduced-motion 가드 유지.
8. **ChatMockup 창 크롬 정돈**: 타이틀바에 창 제목 텍스트 + 그림자 소폭 강화.

## 3. 지키는 것 (변경 금지)
- 검증 계약 문자열: "지금 시작하기"·"출처가 달립니다"·"규정에서 확인되지 않습니다"·"규정 원문"·"6자리 코드"·"질문하러 가기"·섹션/레일 6개.
- 새니타이즈 원칙(docs/36 §5): 실규정 텍스트·수치 인용 금지 — 수치는 빌드타임 집계(counts)만.
- 보안: /about 카드 무링크(docs/44 §3)·비로그인 게이트 불변. KRDS 토큰 외 색 금지(그라디언트도 토큰 파생).
- prefers-reduced-motion: 애니메이션 미정의 기존 패턴.

## 4. 수용 기준
- verify-landing 24/24 그대로 통과(계약 문자열·6섹션·레일·reduced-motion·375px 무가로스크롤).
- 신규 검사(verify-landing에 추가): 히어로 h1 폰트 ≥52px & 자간 음수, 그라디언트 라인 1곳(-webkit-background-clip:text 요소 1개), 메타 스트립 수치 렌더, 아이브로 5개 넘버링(01~05).
- 실렌더 스크린샷(라이트·다크·375px) 사람 확인.

## 5. 롤백
커밋 revert만(구조 불변·CSS/카피 중심). 플래그 landing_page off 시 기존처럼 '준비 중'.

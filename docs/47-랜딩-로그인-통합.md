# docs/47 — 랜딩·로그인 통합 (소개를 메인으로)

> 2026-07-16. 지시: 소개를 숨길 이유가 없으니 랜딩과 통합. 비로그인 '/'를 로그인 화면이 아니라
> **소개 화면**으로 바꾸고, 로그인 시트는 **같은 위치에 sticky 고정**(스크롤해도 이동 X) —
> 스크롤하면 뒤(옆)에 소개가 주르륵 내려가며 보이게. 디자인은 더 깔끔·명확하게.

## 1. 변경
- 비로그인 `/` 렌더를 **컴팩트 2열(히어로+로그인, 무스크롤)** → **통합 스크롤 랜딩**으로 교체.
  - 왼쪽(`.mergedIntro`): 히어로 + 소개 4섹션(01 질문하기·02 근거·03 둘러보기·04 신뢰)이 스크롤.
  - 오른쪽(`.mergedLogin > .loginSticky`): 로그인 카드 `position: sticky` — 스크롤해도 제자리.
- `/about`(변형 `full`)은 그대로 유지(외부 공유용 로그인 없는 내러티브). 홈의 '서비스 소개 자세히 보기' 링크 제거(소개가 인라인이라 불필요).
- counts를 `index.tsx` getStaticProps → `Assistant` → `Landing(variant="home")`로 전달(수치 스트립·통계 실측).

## 2. sticky 구현의 함정 (수정 기록)
- `position: sticky`가 안 먹던 원인: 그리드에 `align-items: start`를 줘서 **로그인 컬럼이
  콘텐츠 높이로 붕괴** → sticky가 이동할 세로 공간이 없었다.
- 해결: `align-items`를 기본값(stretch)으로 → 로그인 컬럼이 소개 높이만큼 늘어나 sticky가
  헤더 아래(`top: header+20px`)에 고정된 채 소개만 스크롤.
- 실측: 스크롤 700px에도 카드 y 96→76(불변, ✅). 다크·라이트·1900/1440/375 전부 확인.

## 3. "깔끔·명확" 디자인
- 소개 섹션의 **풀블리드 컬러블록(mBlue/mGreen…) 제거** → 테마 배경 + 섹션 상단 **얇은 구분선**만.
- 그라디언트는 히어로 한 줄만, **미드톤(#3182f6→#8b5cf6→#e0508a)** 으로 교체 — 라이트/다크 양쪽 대비 확보(통합 홈은 테마 배경이라 기존 다크 전용 톤은 라이트에서 흐렸음).
- 넘버링 아이브로(01~04)·초대형 타이포(clamp 44~84px)·모노 메타 스트립·라이브 펄스 점 유지.
- 모바일(≤900px): 단일 컬럼, **로그인 먼저**(order:-1) 후 소개, sticky 해제. 375px 가로 스크롤 0.

## 4. 수용 기준 (verify-landing 28/28)
- ⑦ 통합 랜딩: 히어로 문구("규정이 답합니다")와 로그인 폼이 한 화면에 공존.
- ⑦ 로그인 첫 화면 가시 · ⑨ 스크롤 700 후 로그인 카드 y 불변(sticky, 1900·1440).
- ⑥ flag off '/': 기존 Login 폼(히어로 문구 미노출) · /about '준비 중'.
- 회귀: now 22/22 · secure 37/37(공개 '/' 게이트 계약 불변) · branding · 375px 무가로스크롤.

## 5. 롤백
커밋 revert만. `/about` 무변경이라 소개 자체는 항상 보존.

## 6. v2 (2026-07-16, 같은 날 개선) — 슬라이드 스냅 + 우중앙 완전 고정
사용자 피드백: ①첫 스크롤 틱에 로그인이 한 번 위로 덜컹(왕복 시 계속) ②로그인을 더 오른쪽·소개
폭 확대 ③일렬 나열 대신 "점수판 넘기듯" 슬라이드 스크롤(지나간 내용은 안 보이게, 집중).

- **구조 교체**: 페이지 스크롤 자체를 제거(한 화면 고정, 구 compact 공식) — 소개 컬럼만
  내부 스크롤 컨테이너(`overflow-y:auto` + `scroll-snap-type:y mandatory` + `snap-stop:always`).
  로그인은 우측 컬럼 세로 중앙(`align-self:center`) — **레이아웃상 부동이라 덜컹임이 구조적으로 불가능**
  (실측: 초기→틱1→역방향→재전진 y=157 전부 동일).
  - v1 덜컹임 원인: sticky 자연위치(y96)와 고정위치(top: header+20 → y76)의 20px 차 — 스크롤
    0↔1틱 경계에서 두 위치를 오갔음.
- **슬라이드**: `.mHero`/`.mSection` = 컨테이너 높이 100% 1장씩(flex 세로 중앙), 지나간 슬라이드는
  잘려서 안 보임. 스크롤바 숨김(휠·터치·키 유지), 히어로에 `SCROLL ▾` 힌트.
- **휠 훅(useSlideWheel)**: 마우스 휠 1틱(~120px)은 CSS 스냅 임계(슬라이드 ½)를 못 넘어 스냅백
  → 휠을 가로채 **1틱 = 1슬라이드** `scrollTo({smooth})`(620ms 락, reduced-motion은 auto).
  mandatory일 때만 개입 — 낮은 화면(≤700px h, proximity 폴백)·모바일(스냅 해제)은 네이티브.
  실측: 틱1→694(=1장 정합)·틱2→1388·역틱→694.
- 폭: 로그인 컬럼 380→360px·gap 축소 → 소개 폭 확대. 슬라이드 안 비주얼 컴팩트(graphViz 170px 등).
- 검증 ⑨ 교체: 페이지 무스크롤 + 휠1틱 스냅 정합 + 로그인 y 부동(1900·1440) — 28/28.

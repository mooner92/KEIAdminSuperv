# 1) HWP/HWPX → Markdown (순수 파이썬, JVM 불필요)
hwp-hwpx-parser>=1.0.0
# (대안) pyhwp  # hwp5txt/hwp5html, HWP5 전용

# 2) 청킹 + 임베딩 + 벡터DB
sentence-transformers>=3.0.0   # KURE-v1 / BGE-M3 로드
chromadb>=0.5.0                # 로컬 벡터 스토어
kss>=6.0.0                     # (선택) 한국어 문장 분할

# torch: GPU 드라이버에 '맞는' CUDA 빌드를 설치해야 한다(중요).
#   기본 PyPI 휠은 최신 CUDA(예: cu130)라 구형 드라이버(R535/CUDA 12.2)에선 CUDA 인식 실패.
#   드라이버가 CUDA 12.x면 cu124 휠로 설치:
#     pip install torch --index-url https://download.pytorch.org/whl/cu124
#   (nvidia-smi 의 'CUDA Version'으로 드라이버 지원 한도를 먼저 확인)
torch>=2.6.0

# 3) RAG 질의 (로컬 vLLM = OpenAI 호환 엔드포인트)
openai>=1.40.0

# 4) 배포: RAG를 OpenAI 호환 API로 (Open WebUI 연결용)
fastapi>=0.115.0
uvicorn>=0.30.0

# 5) LLM 앱: 로그인 + 채팅기록 영속화 (SQLite) + 멀티턴 + 메시지별 근거
sqlmodel>=0.0.22   # SQLAlchemy 2.0 + Pydantic 기반 ORM (FastAPI 친화)
pyjwt>=2.9.0       # 세션 쿠키(JWT, HS256)
bcrypt>=4.0        # 비밀번호 해시(직접 사용 — passlib은 bcrypt 5 호환 이슈로 미사용)

# 6) 연구행정 가이드 변환(01c): PDF/PPTX → md
pymupdf>=1.24      # PDF 텍스트 추출(fitz). 스캔 이미지 PDF는 image-pdf로 표시
python-pptx>=1.0   # PPTX 슬라이드 텍스트/표 추출
